[package]
name = "rust-os"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]
members = ["loader", "kernel"]

[lib]
name = "rust_os"
path = "src/lib.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
